# FastAPI 和 Uvicorn
# 注意：Gradio 4.19.0 需要更新版本的 FastAPI（>=0.110.0）以支持 FieldInfo.in_ 属性
fastapi>=0.110.0,<1.0.0
uvicorn[standard]>=0.24.0
# langchain 0.1.0 与 langchain-community 0.4.1 不兼容
# 使用 langchain-classic 和 langchain-community 替代
langchain-classic==1.0.0
langchain-community==0.4.1
langchain-deepseek>=0.1.0
# langchain-deepseek 的依赖（会自动安装，但显式声明以便版本控制）
langchain-openai>=0.1.0  # langchain-deepseek 依赖此包
openai>=1.0.0  # langchain-openai 依赖此包
# pydantic 需要 >=2.7.4 以满足 langchain-classic 的要求
# 同时需要 <3.0.0 以满足 fastapi 的要求
pydantic>=2.7.4,<3.0.0
python-dotenv==1.0.0
# httpx 需要与 FastAPI 兼容的版本
httpx>=0.25.2
# Gradio UI 依赖
# 使用 Gradio 4.x 版本（让 pip 自动选择最新兼容版本）
# 注意：Gradio 4.x 可能尝试导入 HfFolder，但新版本的 huggingface_hub 已移除
# 解决方案：代码中已添加兼容性修复（HfFolder 兼容类）
# 如果遇到版本问题，可以尝试：pip install gradio --upgrade
gradio>=4.0.0
requests>=2.31.0

